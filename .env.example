# Copy this file to .env and fill in real values. Never commit .env itself.

# Pooled Neon connection string (Dashboard -> Connection Details -> Pooled connection)
DATABASE_URL=postgresql://user:password@ep-example.region.aws.neon.tech/neondb?sslmode=require

# Generate any random string for local dev; use the same value you configure
# in GitHub's webhook settings when you set up the actual webhook.
WEBHOOK_SECRET_GITHUB=replace-with-a-random-secret

PORT=8080

# Neon requires SSL, so leave this as true for deployed/Neon use.
# Set to false only if pointing at a local Postgres without SSL.
DB_SSL=true
