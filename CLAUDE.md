# Working in this repo

- Do not run `npm run start:dev`, `npm start`, or otherwise launch the app after making file changes. Do not restart or kill dev server processes. The user runs and tests the app themselves — just make the code changes and stop there.
- `npm run lint` and `npm run build` (type-check only, no run) are fine to use for verification.
